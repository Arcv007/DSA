// write a program to print the subsets of a string using recursion
#include<bits/stdc++.h>
using namespace std; 

void subs(string str)
{
    
}

int main()
{
    string str;
    cout<<"Enter the string";
    cin>>str;
    subs(str);
    return 0;
}
